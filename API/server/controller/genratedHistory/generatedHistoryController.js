const express = require("express");
const Words = require("../../models/schemas").default;
const asyncHandler = require("express-async-handler");

const getWords = asyncHandler(async (req, res) => {
  try {
  } catch (error) {}
});

const getWordTypes = asyncHandler(async (req, res) => {
  try {
  } catch (error) {}
});

const getGeneratedHistory = asyncHandler(async (req, res) => {
  try {
  } catch (error) {}
});

module.exports = { getWords, getWordTypes, getGeneratedHistory };
