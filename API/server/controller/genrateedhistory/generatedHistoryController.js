const express = require("express");
const Words = require("../../models/schemas");
const asyncHandler = require("express-async-handler");

const getGeneratedHistory = asyncHandler(async (req, res) => {
  try {
  } catch (error) {}
});

module.exports = { getGeneratedHistory };
